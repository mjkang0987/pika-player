package com.pikaworks.pikaplayer.ui.pro

/**
 * Pro 로 무엇을 얻는지 알리는 문구의 유일한 출처.
 *
 * 구매 화면과 설정 화면이 같은 목록을 쓴다. 두 군데에 따로 적어 두면 한쪽만 고쳐져
 * "설정에서 본 것과 구매 화면에 적힌 것이 다르다" 가 된다. 파는 물건의 설명이
 * 화면마다 다른 것은 기능 버그보다 신뢰를 크게 깎는다.
 *
 * 등급 판단 자체는 `:entitlement` 의 Feature 가 하고, 여기는 표기만 맡는다.
 */
object ProFeatures {

    /** 결제 없이 되는 것. 구매 화면에서 먼저, 전부 체크로 보여준다. */
    val free = listOf(
        "전 포맷 재생 · 하드웨어 가속",
        "자막 표시 · 인코딩 변경 · 싱크 조정",
        "제스처 · 재생속도 · 화면비",
        "이어보기 · 폴더 탐색 · 검색",
        "광고 없음",
    )

    /**
     * Pro 로 열리는 것. 앞쪽일수록 전환 기여가 큰 항목이다 — 기획서 3장의
     * "주력은 네트워크 연동 + PiP + 비공개 폴더" 순서를 따른다.
     */
    val pro = listOf(
        "화면 속 화면(PiP) · 자동 작은 창",
        "네트워크 스트리밍 (SMB · NAS)",
        "비공개 폴더 · PIN 잠금",
        "클라우드 연동 · Wi-Fi 전송",
        "Chromecast · 어린이 잠금",
    )

    /**
     * 설정 화면 카드에 넣을 한 줄 요약.
     *
     * [pro] 에서 기계적으로 뽑지 않는다 — 항목 이름을 그대로 이으면 문장이 어색해진다.
     * 대신 바로 옆에 둬서 목록을 고칠 때 이것도 눈에 들어오게 한다.
     */
    const val shortPitch = "작은 창 · 네트워크 폴더 · 비공개 폴더"
}
